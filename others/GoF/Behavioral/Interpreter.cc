/*****************************************************************************
 *解释器模式：给定一个语言，定义它的文法的一种表示，并定义一个解释器，这个解释
 *            器使用该表示来解释语言中的句子。
 *（1）如果一种特定类型的问题发生的频率足够高，那么可能就值得将该问题的各个实例
 *     表述为一个简单语言中的句子。这样就可以构造一个解释器，该解释器通过解释这
 *     些句子来解决该问题。
 *（2）当有一个语言需要解释执行，并且你可以将该语言中的句子表示为一个抽象语法树
 *     时，可以使用解释器模式。
 *
 *
 * ***************************************************************************
 */
